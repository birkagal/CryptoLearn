import React from "react";
import Block from "../components/Block";

/* Hash View using Block component to render basic Hash page. Use no flags to display basic Hash page. */
const HashView = () => {
  return <Block page_title={"SHA256 Hash"} flags={{}} />;
};

export default HashView;
